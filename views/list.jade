extends partials/layout

append head
  title IestynWilliams.net | The website of Iestyn Williams

block body
  each post in posts
    if !post.page
      article
        a(href=post.url)
          h1= post.title
        != post.content