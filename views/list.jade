extends partials/layout

block body
  ul.post-list
    if posts.length
      each post in posts
        if !post.page
          li
            a(href=post.url)= post.title
    else
      :markdown
        Add a markdown file to `./posts` to get started!